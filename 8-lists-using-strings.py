
# Ordered sequence of different types of values.

user_name='Srikanth'                                       #This is to store the single value in a variable.
user_list=['Srikanth','Lasya','Nainika','Apple','Ball']    #Defining multiple values in a varaible as a list of values.
#print(user_list)
#print(user_list[0])                         #To print the first value in the list, because the lists are stored in index format.
#print(user_list[2])

#user_list.append('New-name')     #To add a new value in the list.
#print(user_list)

#user_list.remove('Lasya')        #To delete a value in the list.
#print(user_list)

#del user_list[1]                  #To delete a value in the list by index.
#print(user_list)

#user_list[1]='LASYA'             #To modify a value in the list.
#print(user_list)

#user_list.insert(1, 'sri')       #To insert a new value in a specific position, we use indexing for placing a new value.
#print(user_list)

#print(len(user_list))            #To get the length of the lists in the variable.

#user_list.sort()                 #To get the values in the sorted format.
#print(user_list)

#user_list.sort(reverse=True)     #To get the values in the reverse format.
#print(user_list)

#user_list.reverse()              #To print the values in a reverse format.
#print(user_list)

#user_list.pop()                  #This is used to remove the last value in a list and stores it as a output to the next input.
#print(user_list)

#new_value=user_list.pop()        #This is used to remove the last value in a list and stores it as a output to the next input.
#print(new_value)

#new_value=user_list.pop(1)       #This is used to remove the last value in a list and stores it as a output to the next input by taking index as a value.
#print(new_value)
#print(user_list)

print(user_list[0:2])             #To get the range of values. We use indexing for getting the values.

print(user_list[-2])              #To get the range of values from last. We use indexing for getting the values.
